const Login = () => {
    const signIn = () => {
        console.log("SignIn");
    }
    return (
        <div>
            <button onClick={signIn}>Login</button>
        </div>
    );
}

export default Login
